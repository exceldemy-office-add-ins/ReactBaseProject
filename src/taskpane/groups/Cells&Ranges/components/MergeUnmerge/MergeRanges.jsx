import React from 'react'

export default function MergeRanges() {
    const mergeUnmerge = async () => {
        try {
          await Excel.run(async (context) => {
            
            
          });
        } catch (error) {
          console.error(error);
        }
      };
  return (
    <div>
    merge
    </div>
  )
}
